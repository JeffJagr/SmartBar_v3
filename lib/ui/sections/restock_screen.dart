import 'package:flutter/material.dart';

class RestockScreen extends StatelessWidget {
  const RestockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Restock Screen (Coming Soon)'),
    );
  }
}
