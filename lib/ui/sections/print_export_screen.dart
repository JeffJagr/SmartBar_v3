import 'package:flutter/material.dart';

class PrintExportScreen extends StatelessWidget {
  const PrintExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Print / Export - Coming Soon'),
    );
  }
}
