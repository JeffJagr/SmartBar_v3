import 'package:flutter/material.dart';

class OrdersSectionScreen extends StatelessWidget {
  const OrdersSectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Orders Screen (Coming Soon)'),
    );
  }
}
