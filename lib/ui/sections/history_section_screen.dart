import 'package:flutter/material.dart';

class HistorySectionScreen extends StatelessWidget {
  const HistorySectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('History Screen (Coming Soon)'),
    );
  }
}
